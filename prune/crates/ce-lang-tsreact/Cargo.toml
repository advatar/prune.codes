[package]
name = "ce-lang-tsreact"
version = "0.1.0"
edition = "2021"
license = "MIT"

[dependencies]
anyhow = "1"
ce-core = { path = "../ce-core" }

# Tree-sitter parsing
# Note: grammar crates expose `LanguageFn` constants and do not hard-pin the tree-sitter version.
tree-sitter = "0.22"
tree-sitter-typescript = "0.23"
